" Splitjoin config, todo: move to its own file

" Don't add curly braces to options hashes.
let g:splitjoin_ruby_curly_braces = 0
" When splitting args, indent twice (don't align with method call)
let g:splitjoin_ruby_hanging_args = 0
